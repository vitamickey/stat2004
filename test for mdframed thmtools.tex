\documentclass[a4paper,12pt]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amsfonts,amssymb,amsthm}
\usepackage[dvipsnames]{xcolor}
\usepackage[framemethod=TikZ]{mdframed}
\usepackage{thmtools,tikz}

\declaretheoremstyle[
    headfont=\bfseries, 
    notefont=\scshape, notebraces={(}{)},
    bodyfont=\itshape,
    postheadspace=1em,
    % postheadhook={\textcolor{red}{\rule[.6ex]{\linewidth}{0.4pt}}\\},
    spacebelow=\topsep,
    % spaceabove=\topsep,
    mdframed={
            % linecolor=black!100, 
            linewidth=2pt,
            innerbottommargin=6pt, 
            skipabove=\topskip, 
            skipbelow=\topskip,
            topline=false,
            bottomline=false, 
            rightline=false
            }
]{thm}

\declaretheorem[
    style=thm,
    name=Theorem, 
    within=section,
]{theorem}



\begin{document}

\section{SEction numero uno}

\begin{theorem}[Here is an example of a theorem yo]
    Blah blah squeeze theorem idk. \[b_n \leq a_n \leq c_n\] must mean that the limit of \(a_n\) is between the limits of \(b_n\) and \(c_n\). 
\end{theorem}

\begin{proof}
    I just use the latex pre-defined proof environment. 
\end{proof}

\end{document}
